[setup]
nixPkgs = ["...", "ffmpeg"]

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "nix-env -iA nixpkgs.ffmpeg && pip install git+https://github.com/mps-youtube/pafy.git  && python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3