[setup]
nixPkgs = ["...", "ffmpeg"]
builder = "NIXPACKS"

[deploy]
startCommand = "pip install git+https://github.com/mps-youtube/pafy.git  && python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3